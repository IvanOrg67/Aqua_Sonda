import 'package:flutter/material.dart';

class AppColors {
  // Fondo general (claro)
  static const bg = Color(0xFFF7F9FC);

  // Superficie/Cartas (claro)
  static const surface = Color(0xFFFFFFFF);

  // Primarios (azules)
  static const primary = Color(0xFF2D6CDF);
  static const primaryAlt = Color(0xFF19B4FF);

  // Texto (oscuro para fondo claro)
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);

  // Bordes / líneas (claro)
  static const border = Color(0xFFE5E7EB);

  // Estados
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // Degradado para fondo de login
  static const gradientStart = Color(0xFF0F141A);
  static const gradientEnd = Color(0xFF0E2A5D);
}

class AppColorsDark {
  // Fondo general (oscuro)
  static const bg = Color(0xFF0F141A);

  // Superficie/Cartas (oscuro)
  static const surface = Color(0xFF1B222B);

  // Primarios (azules)
  static const primary = Color(0xFF2D6CDF);
  static const primaryAlt = Color(0xFF19B4FF);

  // Texto (claro para fondo oscuro)
  static const textPrimary = Color(0xFFE6EDF3);
  static const textSecondary = Color(0xFF9AA7B2);

  // Bordes / líneas (oscuro)
  static const border = Color(0xFF2B333D);

  // Degradado para fondo de login
  static const gradientStart = Color(0xFF0F141A);
  static const gradientEnd = Color(0xFF0E2A5D);
}
