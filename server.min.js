const express = require('express');
const app = express();

app.get('/health', (req, res) => {
  res.json({ ok: 1 });
});

const PORT = 3000;
app.listen(PORT, () => {
  console.log(`✅ MINI API en http://localhost:${PORT}`);
});
